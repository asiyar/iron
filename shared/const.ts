/** Oturum çerezinin adı — hem sunucu hem istemci tarafında kullanılır. */
export const COOKIE_NAME = "app_session_token";

/** tRPC uçlarının kök yolu. Gateway yönlendirmesi için "/api/" ile başlamalı. */
export const API_PREFIX = "/api";
